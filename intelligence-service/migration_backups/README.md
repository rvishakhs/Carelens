# Unapplied migration archive

The `.py.txt` file is the discarded, destructive autogeneration output.
It was never applied to the local database and is retained only for reference.
Do not move it into `migrations/versions` or execute it.
